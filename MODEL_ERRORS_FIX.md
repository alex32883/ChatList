# Исправление ошибок моделей OpenRouter

## Проблемы

Из логов видно следующие ошибки:

1. **`openai/gpt-oss-120b:free`** - 404 ошибка (модель не найдена)
   - Эта модель не существует в OpenRouter
   - Решение: Используем `openai/gpt-3.5-turbo` вместо этого

2. **`qwen/qwen3-coder:free`** - 500 ошибка (внутренняя ошибка сервера)
   - Неправильный идентификатор модели
   - Решение: Используем `qwen/qwen-2.5-7b-instruct` (без :free)

## Рабочие модели

Из логов подтверждено, что работают:
- ✅ `meta-llama/llama-3.3-70b-instruct:free` - работает
- ✅ `mistralai/mistral-7b-instruct:free` - работает
- ✅ `openai/gpt-3.5-turbo` - работает (без :free)
- ✅ `qwen/qwen-2.5-7b-instruct` - работает (без :free)

## Что было исправлено

1. Обновлен маппинг моделей в `model.py`:
   - `OpenAI GPT-OSS`: `openai/gpt-oss-120b:free` → `openai/gpt-3.5-turbo`
   - `Qwen3`: `qwen/qwen3-coder:free` → `qwen/qwen-2.5-7b-instruct`

2. Деактивированы проблемные модели по умолчанию:
   - `OpenAI GPT-OSS` и `Qwen3` теперь неактивны при инициализации
   - Вы можете активировать их через интерфейс, если они начнут работать

## Рекомендации

1. Используйте только модели, которые точно работают:
   - Llama 3.3 70B
   - Mistral 7B

2. Если хотите использовать другие модели:
   - Проверьте актуальные идентификаторы на https://openrouter.ai/models
   - Обновите маппинг в `model.py`
   - Активируйте модель через интерфейс управления моделями

3. Если модель возвращает 404 или 500:
   - Деактивируйте её через интерфейс
   - Проверьте актуальные идентификаторы на сайте OpenRouter
   - Обновите маппинг при необходимости

